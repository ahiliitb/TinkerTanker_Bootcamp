#pragma once


namespace GUI {
  // Defining the parameters
  const int GRID_SIZE = 20;
  const int NUM_CELLS_X = 40;
  const int NUM_CELLS_Y = 40;
  const int SCREEN_WIDTH = GRID_SIZE * NUM_CELLS_X;
  const int SCREEN_HEIGHT = GRID_SIZE * NUM_CELLS_Y;
  const int TIME_DELAY = 100;
}
